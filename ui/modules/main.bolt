import xml.etree.ElementTree as ET
from ./builder import Builder
from bolt_expressions import Scoreboard, Data
from nbtlib import Float

dummy = Scoreboard("coc.dummy")
atSelf = Data.entity("@s")

append function coc:modules/in_world_ui/track_player:
    rotation = atSelf.Rotation
    rotation[0] = dummy["$r"] / 1000
    tp @s ~ ~-0.001 ~

for name, content in ctx.meta.ui.all_screens.items():
    Builder(content, ctx)
    